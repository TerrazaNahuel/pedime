[project]
name = "pedime"
version = "1.0"

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "S"]
ignore = ["E501", "N818"]

[tool.ruff.lint.per-file-ignores]
"backend/tests/*" = ["S101", "S107"]
"backend/alembic/*" = ["E402"]
